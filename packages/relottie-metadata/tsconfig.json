{
  // Extend from the build config
  "extends": "../../tsconfig.dev.json",

  // Compiler options
  "compilerOptions": {
    "resolveJsonModule": true,
    "types": ["jest", "node"]
  },

  "ts-node": {
    "esm": true
  }
}
