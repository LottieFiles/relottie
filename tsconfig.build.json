{
  // Extend from the org config
  "extends": "@lottiefiles/tsconfig",

  "compilerOptions": {
    // Target ES version
    "target": "ESNext",

    // Target module system
    "module": "ESNext",

    // Silence TS 6.0 deprecations for options coming from org tsconfig / tsup-injected baseUrl.
    // Remove once @lottiefiles/tsconfig drops downlevelIteration/moduleResolution=node10.
    "ignoreDeprecations": "6.0"
  },

  "exclude": [
    // Exclude node_modules
    "**/node_modules",

    // Exclude tests and snapshots
    "**/test",
    "**/*.test.ts",
    "**/__fixtures__",
    "**/__mocks__",
    "**/__snapshots__",
    "**/__tests__",
    "**/coverage",

    // Exclude builds
    "**/build",
    "**/dist",
    "**/artifacts"
  ]
}
